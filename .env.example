# Example .env file for probir project

# --- For mitmproxy addon (probir.py) ---
# Comma-separated list of target domains to capture traffic from.
# Example: TARGET_DOMAINS=generativelanguage.googleapis.com,api.openai.com
TARGET_DOMAINS=

# --- For LLM-based pipeline steps (e.g., step1b, step3) ---
# These are optional. If not set, defaults in scripts or CLI args will be used.
# CLI arguments take precedence over these environment variables.
# .env file values are loaded if environment variables are not already set.

# Default Ollama model to use if not specified by CLI
# Example: OLLAMA_MODEL=phi3:mini
OLLAMA_MODEL=phi3:mini

# Default Ollama host URL if not specified by CLI
# Example: OLLAMA_HOST=http://localhost:11434
OLLAMA_HOST=http://localhost:11434
